# packages/shared-types

Shared type/contract definitions used across apps and services (e.g. API request/response shapes) that aren't part of the domain evidence model itself — see `packages/evidence-schema` for that.

## Status

Placeholder. No code yet.

# packages/ui-components

Shared UI component library for `apps/web` (and any future analyst-facing frontend), keeping visual/interaction patterns consistent across the console.

## Status

Placeholder. No code yet.

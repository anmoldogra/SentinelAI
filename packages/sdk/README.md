# packages/sdk

Client SDK for programmatic access to the SentinelAI API (via `apps/server/entrypoints/http`), for external tooling and integrations.

## Status

Placeholder. No code yet — planned for Phase 4 (see `docs/roadmap.md`).

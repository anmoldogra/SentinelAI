# infra/docker

Dockerfiles and container build configuration for each app/service, plus any shared base images. The root `docker-compose.yml` currently provisions only third-party infrastructure dependencies (Postgres, Redis, Redpanda, MinIO, Qdrant) for local development; per-service Dockerfiles will live here as each service is implemented.

## Status

Placeholder. No content yet.

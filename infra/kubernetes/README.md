# infra/kubernetes

Kubernetes manifests/Helm charts for deploying SentinelAI apps and services to a cluster (staging/production).

## Status

Placeholder. No content yet — deployment target and manifest strategy (raw manifests vs. Helm vs. Kustomize) to be decided via ADR once Phase 1 services exist to deploy.

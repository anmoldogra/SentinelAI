# tests/e2e

End-to-end tests exercising full user-facing flows across `apps/web` and the API gateway (e.g. "analyst creates a case, ingests evidence, sees an AI-generated correlation"). Complements unit/integration tests that live alongside each service's own code once written.

## Status

Placeholder. No tests yet.

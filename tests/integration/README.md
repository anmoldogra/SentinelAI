# tests/integration

Cross-module integration tests that verify contracts between `apps/server` modules (e.g. `ingestion` → `case-management` evidence linking, `investigation` reading across domain modules) without going through the full UI. These same tests are what should keep passing, unmodified, if a module is later extracted into its own service per `apps/server/README.md`.

## Status

Placeholder. No tests yet.
